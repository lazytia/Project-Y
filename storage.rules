rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Each authenticated user can read/write their own onboarding documents
    match /staff_onboarding/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
